## 比重
假设部分值的现期值为 $A$，增长率为 $a\%$；总体值的现期值为 $B$，增长率为 $b\%$。
### 两期比重差
$$\frac{A}{B} - \frac{\frac{A}{1 + a\%}}{\frac{B}{1 + b\%}} = \frac{A}{B}\times(1 - \frac{1 + b\%}{1 + a\%}) = \frac{A}{B} \times \frac{a\% - b\%}{1 + a\%}$$
### 两期比重比较
根据 $a\% - b\%$ 的值来确定比重是增是减。